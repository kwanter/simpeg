# Task 5 Implementer Report: Dependency Hygiene

**Commit:** 26ccdd4 `chore(deps): remove unused intervention/predis/sanctum/breeze, drop database wildcard`
**Branch:** arch-remediation | 7 files, +2 −375

## Grep evidence BEFORE

Command: `grep -rn 'Intervention\|Image::make\|predis\|Predis\|auth:sanctum\|createToken\|HasApiTokens\|Sanctum' app/ routes/ config/ bootstrap/ --include='*.php'`

Hits:
- `app/Models/User.php:11,17` — HasApiTokens import + trait (expected)
- `routes/api.php:17` — auth:sanctum /user route (expected)
- `config/sanctum.php` — auto-published config (expected)
- `app/Http/Kernel.php:44` — COMMENTED-OUT EnsureFrontendRequestsAreStateful line (dead comment; not usage)
- `config/app.php:178` — **Intervention\Image\ImageServiceProvider registration (NOT in expected list)** — scaffolding registration, zero API usage anywhere; removed as part of dep removal
- `config/app.php:196` — **'Image' => Intervention facade alias (NOT in expected list)** — same, removed
- `config/database.php:118` — false positive (`phpredis` contains substring `predis`); kept
- `bootstrap/cache/services.php`, `bootstrap/cache/packages.php` — stale compiled caches; cleared (`rm -f bootstrap/cache/*.php`), regenerated by package:discover

No `createToken`/controller usage found → not BLOCKED. tests/ and database/migrations: zero references.

## Changes

1. `composer.json`: removed `intervention/image`, `predis/predis`, `laravel/sanctum` (require); `laravel/breeze` (require-dev); `illuminate/database: "*"` wildcard.
2. `app/Models/User.php`: removed `use Laravel\Sanctum\HasApiTokens;` + trait from use statement.
3. `routes/api.php`: removed sanctum /user route + now-unused `Request`/`Route` imports; file + header comment kept (RouteServiceProvider still loads it).
4. `config/app.php`: removed Intervention provider + 'Image' alias (deviation from task's expected-hits list — required; see below).
5. `app/Http/Kernel.php`: removed dead commented-out Sanctum middleware line.
6. `config/sanctum.php`: **DELETED** (deviation — task said "may remain; harmless". It was NOT harmless: sanctum 3.x published config calls `Sanctum::currentApplicationUrlWithPort()` eagerly at config load → `Class "Laravel\Sanctum\Sanctum" not found` fatal on every boot/artisan/config:cache after composer remove. Deleting is the only correct fix.)
7. `database/migrations/2019_12_14_000001_create_personal_access_tokens_table.php`: kept untouched per instructions (harmless history).

## Composer output

- `composer update intervention/image predis/predis laravel/sanctum laravel/breeze --with-all-dependencies` (packages already dropped from composer.json by manual edit, per task steps 2+5) — lock updated, autoload regenerated.
- First run failed post-autoload-dump: `artisan package:discover` fatal masked by `app/Exceptions/Handler.php:31` (Log facade before boot). Root cause exposed via direct bootstrap probe: config/sanctum.php:21. Fixed by deleting that config.
- `composer validate` → `./composer.json is valid`.
- `composer audit` → 17 advisories / 4 packages (laravel/framework, dompdf/dompdf, guzzlehttp/guzzle, league/commonmark). **All pre-existing** on kept deps — this is RISK-001 (deferred Laravel upgrade plan) + kept dompdf/guzzle/commonmark versions. None introduced by Task 5; removed packages carried no advisories. Plan's "audit — clean" expectation is stale.

## Lock diff summary

`composer.lock`: 1 insertion / 275 deletions. Packages removed: `intervention/image`, `laravel/sanctum`, `predis/predis`, `laravel/breeze`. illuminate/database wildcard had no separate lock entry (transitive via laravel/framework — stays, correctly).

## Grep evidence AFTER

Same command → only hit: `config/database.php:118: 'client' => env('REDIS_CLIENT', 'phpredis')` (false positive). No live references. bootstrap/cache regenerated clean (no Intervention/Sanctum entries).

## Test + cache evidence

- `php artisan test` → **Tests: 257 passed (502 assertions)** — identical to pre-task baseline (Tasks 1-4).
- `DB_HOST=127.0.0.1 CACHE_STORE=file CACHE_DRIVER=file php artisan config:cache` → "Configuration cached successfully."
- `DB_HOST=127.0.0.1 CACHE_STORE=file CACHE_DRIVER=file php artisan route:cache` → "Routes cached successfully."
- Caches cleared afterward; working tree clean except this reports dir.

## Concerns

1. **Task-context error corrected:** brief claimed intervention/image had "ZERO usage in app/routes/config" — false: config/app.php registered its provider + facade alias (scaffolding, not API usage). Removed; flagged as deviation, not a blocker.
2. **config/sanctum.php deletion required** contrary to "harmless, may remain" — sanctum 3.x config executes Sanctum:: calls at load; keeping it = fatal boot. Deleted.
3. `composer audit` not clean (17 advisories) — pre-existing, RISK-001 territory, explicitly deferred to the Laravel 12.61+ upgrade plan.
4. Redis: client stays `phpredis` (config default, docker .env) — predis removal changes nothing, as predicted.
5. Exception handler quirk noticed (not Task 5 scope): `app/Exceptions/Handler.php:31` Log-facade call in reportable closure masks boot-time errors with "A facade root has not been set" — worth a follow-up ticket.
