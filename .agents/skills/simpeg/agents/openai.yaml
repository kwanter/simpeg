interface:
  display_name: "Simpeg"
  short_description: "Repo-specific patterns and workflows for simpeg"
  default_prompt: "Use the simpeg repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true